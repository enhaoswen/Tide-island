1) MAKE: esc key close every widget and reset to the default clock widget.

2) ENHANCE: app launcher new look, vertical scroll instead of the horizontal scroll.

3) MAKE: wallpaper library to be 2 or 3 row grid with vertical scroll, card view, max of 3 wallpapers per row.

4) FIX: notification center items misplacement.

5) FIX: wallpaper custom command buttons lock.

6) ADD: accept arguments passed to tide-island like the arguments that is passed to the install file,
   such as update and uninstall arguments.

7) ADD: how to update tide-island in the readme file not necessary if idea (6) is done,
   could be replaced with help argument on tide-island command.

8) MAKE: tide-island-config-app have NoDisplay=true,
   cause it will be replaced with the argument on idea (12).

9) FIX: media player glitches,
   when I hold left click on the island and hover over it at the same time.

10) FIX: make swipe right and swipe left instead of the custom page and swipe right (the old way), Done

11) ENHANCE: rename tide-island-config-app to be tide-island-config.

12) ENHANCE: make an argument for tide-island to make it open the config app.
